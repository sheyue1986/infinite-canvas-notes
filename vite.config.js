import { defineConfig } from "vite";

export default defineConfig({
  // Keep all generated references relative so the exported index.html can
  // also be opened directly from a folder.
  base: "./",
});
