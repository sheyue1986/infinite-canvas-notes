# Infinite Canvas Notes

## 直接使用

`dist` 是已经构建好的网页文件。把它部署到任意静态网页服务即可使用。

## 在另一台电脑继续开发

1. 安装 Node.js 22 或更新版本。
2. 在项目文件夹中运行 `npm install`。
3. 运行 `npm run dev`，然后打开终端显示的网址。

## 素材与画布迁移

- 使用 Excalidraw 的菜单导出 `.excalidraw` 文件来迁移画布。
- 当前“我的素材”保存在浏览器本地。素材库导入/导出将作为后续功能加入。
